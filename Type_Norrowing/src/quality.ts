/*  Equality  */
function compareValue(x:string|number, y:string|number) {
    if (x === y) {
        console.log('compareValue');
    } else {
        console.log('Not compareValue');
    }
}
compareValue(3,'3')
